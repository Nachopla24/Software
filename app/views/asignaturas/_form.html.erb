<%= form_with(model: asignatura, local: true) do |form| %>
  <% if asignatura.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(asignatura.errors.count, "error") %> prohibited this asignatura from being saved:</h2>

      <ul>
      <% asignatura.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :asignatura_nombre, class: 'form-control' %>
  </div>

  <div class="field form-group">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion, id: :asignatura_descripcion, class: 'form-control' %>
  </div>

  <div class="field form-group">
    <%= form.label :codigo %>
    <%= form.text_field :codigo, id: :asignatura_codigo, class: 'form-control' %>
  </div>

  <div class="actions form-group">
    <%= form.submit "Guardar", class: 'btn btn-primary btn-lg'%>
  </div>
<% end %>
