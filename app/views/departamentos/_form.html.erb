<%= form_with(model: departamento, local: true) do |form| %>
  <% if departamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(departamento.errors.count, "error") %> prohibited this departamento from being saved:</h2>

      <ul>
      <% departamento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :universidad_id %><br>
    <%= form.select :universidad_id, Universidad.all.collect { |p| [ p.nombre, p.id ] }, include_blank: true %>
  </div>

  <div class="field form-group">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :departamento_nombre, class: 'form-control' %>
  </div>

  <div class="field form-group">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion, id: :departamento_descripcion, class: 'form-control' %>
  </div>

  <div class="actions form-group">
    <%= form.submit "Guardar", class: 'btn btn-primary btn-lg' %>
  </div>
<% end %>
